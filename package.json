{
  "scripts": {
    "start": "http-server --port 5000",
    "format": "prettier --write",
    "server": "json-server --watch db.json"
  },
  "dependencies": {
    "http-server": "^14.1.0",
    "json-server": "^0.17.0",
    "prettier": "^2.6.2"
  }
}
